STAR SCHEMA for Online Sales System

🔹 Concept:
In a Star Schema, we have one central fact table (the measurable events — like Sales),
and multiple dimension tables (the descriptive data — like Customer, Product, Time, Store).

✅ STAR SCHEMA Structure

Fact Table: Fact_Sales

Column Name	Description
Sales_ID	Unique ID for each sales transaction
Customer_ID	References Dim_Customer
Product_ID	References Dim_Product
Store_ID	References Dim_Store
Time_ID	References Dim_Time
Quantity	Quantity sold
Total_Amount	Total sale amount
Discount	Discount applied
Revenue	Final sale revenue

Dimension Tables

Dim_Customer

Field	Description
Customer_ID	Primary key
Customer_Name	Customer’s full name
Gender	Male/Female
Email	Customer email
City	City name
State	State name
Country	Country

Dim_Product

Field	Description
Product_ID	Primary key
Product_Name	Product name
Category	Product category
Brand	Brand name
Price	Unit price

Dim_Store

Field	Description
Store_ID	Primary key
Store_Name	Name of the store
City	Store city
Manager_Name	Store manager’s name

Dim_Time

Field	Description
Time_ID	Primary key
Date	Full date
Day	Day of month
Month	Month name
Quarter	Q1–Q4
Year	Year
🧠 Star Schema Features

Simple and easy to understand

High query performance (few joins)

Denormalized (data redundancy possible)

Ideal for OLAP and data warehouse queries

❄️ 2️⃣ SNOWFLAKE SCHEMA for Online Sales System

🔹 Concept:
The Snowflake Schema is an extension of the Star Schema where dimension tables are normalized into multiple related tables to reduce redundancy.

✅ SNOWFLAKE SCHEMA Structure

Fact Table: Fact_Sales — same as before.

Dim_Product is normalized into:

Dim_Product

Dim_Category

Dim_Brand

Dim_Customer is normalized into:

Dim_Customer

Dim_Location (stores city, state, country)

Example Structure:

Fact_Sales

(Sales_ID, Customer_ID, Product_ID, Store_ID, Time_ID, Quantity, Total_Amount, Revenue)


Dim_Customer

(Customer_ID, Customer_Name, Gender, Email, Location_ID)


Dim_Location

(Location_ID, City, State, Country)


Dim_Product

(Product_ID, Product_Name, Category_ID, Brand_ID, Price)


Dim_Category

(Category_ID, Category_Name)


Dim_Brand

(Brand_ID, Brand_Name)


Dim_Store

(Store_ID, Store_Name, City, Manager_Name)


Dim_Time

(Time_ID, Date, Day, Month, Quarter, Year)

🧠 Snowflake Schema Features
Feature	Description
Normalization	Reduces redundancy by splitting dimensions
More Joins	Slower performance due to multiple joins
Better Data Integrity	Ensures consistency
More Complex Queries	But efficient storage
🧩 Visual (Text-Based Representation)
               ┌──────────────┐
               │  Dim_Customer│
               └──────┬───────┘
                      │
               ┌──────▼──────┐
               │ Dim_Location│
               └─────────────┘

   ┌────────────┐       ┌────────────┐       ┌──────────────┐
   │ Dim_Store  │       │ Dim_Time   │       │ Dim_Product  │
   └────────────┘       └────────────┘       └──────┬───────┘
                                                ┌────▼────┐
                                                │Dim_Brand│
                                                └─────────┘

   ┌───────────────────────────────┐
   │          Fact_Sales           │
   │ (FKs: Customer_ID, Product_ID,│
   │ Store_ID, Time_ID, Quantity...)│
   └───────────────────────────────┘

🏁 Summary Table
Schema Type	Structure	Advantages	Drawbacks
Star Schema	Central fact + denormalized dimensions	Simple, fast queries	Redundancy
Snowflake Schema	Central fact + normalized dimensions	Less redundancy, integrity	More joins, slower
